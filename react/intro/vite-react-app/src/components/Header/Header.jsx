const Header = () => {
  const topic = "React";
  // Write the JS here BEFORE the return keyword
  return (
    <header>
      <h1>Intro to {topic}</h1>
      <p>We are learning React</p>
    </header>
  );
};

export default Header;
